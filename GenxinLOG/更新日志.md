20260609-231418

1. 修复 AI 会话“保存到章节库”后，首章空白章节的右侧章节工具栏变成不完整工具栏的问题。
2. 修复保存后缺少“保存为正式章节”“查看记忆”等依赖章节 frontmatter 的按钮问题。
3. 补齐 AI 会话将内容追加/覆盖到当前已打开章节时的预览正文同步，避免出现同类工具栏状态错乱回归。

20260609-232619

1. 删除 AI 会话深度章节生成后续阶段的 2200-3200 字硬性限制，审稿、返修和最终去 AI 味阶段不再因为字数区间强制重写或中止。
2. 保留正文初稿阶段的目标字数要求，初稿仍会要求接近完整章节长度，过短初稿仍会扩写补足。
3. 移除 AI 会话流式输出的旧全文硬截断提示，避免长正文因固定上限直接停止；重复输出检测仍然保留。

20260609-234813

1. 参考 stop-slop 思路，新增中文小说专用去 AI 味补充规则，强调保留剧情、角色声线、对白毛边、叙事节奏和潜台词。
2. 去 AI 味处理新增小说适配约束：不按非虚构文章规则硬删副词，不强制压缩到固定字数区间，不把对白压平为说明书。
3. 深度章节生成的最终去 AI 味阶段同步接入同一套小说规则，保证独立去 AI 味与章节生成末段的风格要求一致。

20260609-235912

1. 在左下角底栏新增“使用说明”入口。
2. 点击“使用说明”会打开飞书使用文档，方便直接查看软件说明。

20260610-002548

1. 回退 AI 会话“保存到章节库”为旧行为：点击后不再弹出保存方式选择框。
2. AI 会话保存章节时改回自动保存到下一章，避免首章保存后工具栏状态异常的问题再次出现。
20260610-0122

1. 修复本地 `Claude Code CLI` / `Codex CLI` 无法正确继承本机环境的问题，启动时会显式带上本机 PATH、HOME、USERPROFILE、APPDATA 以及 HTTP/HTTPS/ALL/NO_PROXY 代理变量。
2. 修复本地 CLI 模式会被软件预设默认模型覆盖的问题；当未手动填写模型时，软件会读取本机 `~/.claude/settings.json` 与 `~/.codex/config.toml` 中的当前默认模型，并优先按本地 CLI 配置运行。
3. 补充本地 CLI 配置读取、空模型回退、以及 CLI 启动参数的回归测试，避免后续再次出现”本地环境读不到”或”走不到本地代理模式”的回退。

20260611-062349

1. 修复大纲细化生成功能提示”当前项目还没有可用大纲”，但界面却显示大纲文件列表的矛盾问题。
2. 统一大纲检查逻辑，只要 `wiki/outlines` 目录下有 `.md` 文件，就可以进行细化生成，不再强制要求文件包含特定 frontmatter。
3. 修复 AI 会话深度章节生成在”阶段4：AI审稿”时容易中断或长时间卡住的问题，将审稿各阶段超时时间从 2 分钟延长到 5 分钟。
4. 修复审稿失败时可能出现的 `Cannot read properties of undefined (reading 'length')` 报错，增加异常保护确保流程继续。
5. 修复审稿阶段超时组合逻辑，确保内外超时信号都生效，避免无限等待。
6. 增加审稿阶段自动重试机制，单个阶段失败时自动重试最多 2 次，提高网络波动容错性。
7. 改进审稿 JSON 解析失败时的错误日志，方便排查问题。
8. 修复”阶段4：AI审稿”思考输出不流式显示的问题，现在审稿过程会实时流式输出，用户不会误以为卡住。
9. 修复新建 AI 会话或切换会话时，新会话会显示上一个会话思考内容的问题，现在每次新建或切换会话都会清空流式输出状态。
10. 将”深度章节生成”改名为”深度思考”，将”修改模式”改名为”普通模式”和”编辑章节”，功能更清晰。
11. 深度思考和普通模式互斥切换，点击一个会自动激活对应模式，普通模式下可以正常对话不走深度思考流程。
12. 保留独立的”编辑章节”功能按钮，用于读取和修改章节文件。
